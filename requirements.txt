# Dependencies are declared in pyproject.toml. This file is kept so that older
# instructions still work.
#
#   pip install -e .              core: draw.io to C4 conversion
#   pip install -e ".[vision]"    adds --from-image support
#   pip install -e ".[dev]"       adds pytest, ruff, mypy
#
-e .
